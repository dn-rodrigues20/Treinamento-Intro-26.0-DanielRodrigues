info:
  name: Criar Categoria
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/categorias
  body:
    type: json
    data: |-
      {
        "nome": "Aa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
