info:
  name: Cadastrar Usuário
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/api/auth/sign-up/email
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
